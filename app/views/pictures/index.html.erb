<%= link_to "Add a New Picture", "/pictures/new" %>

<ul>
<% @pictures.each do |picture| %>
  <li>
    <%= link_to image_tag(picture.url, :width => 50), "/pictures/#{picture.id}" %>
  </li>
<% end %>
</ul>

    