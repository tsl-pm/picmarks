<ul>
<% @pictures.each do |picture| %>
  <li>
    <%= link_to image_tag(picture.url, :width => 50), picture_url(picture.id) %>
    <%= link_to "Delete", delete_picture_url(picture.id), :method => 'delete', :confirm => "Whoa dude. Are you sure?" %>
  </li>
<% end %>
</ul>